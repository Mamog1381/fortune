# ========================================
# فایل .env برای دیپلوی روی VPS
# این فایل رو کپی کن به .env و مقادیر رو پر کن
# ========================================

# ===== 🔥 CRITICAL - باید حتماً پر کنی =====

# Django Secret Key (تولید کن با دستور زیر):
# python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=

# Debug Mode (حتماً False برای production)
DEBUG=False

# دامنه‌ها و IP های مجاز (با کاما جدا کن)
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,YOUR_VPS_IP

# Database URL (پسورد قوی انتخاب کن!)
DATABASE_URL=postgresql://postgres:YOUR_STRONG_DB_PASSWORD@db:5432/coffee_fortune_db

# پسورد دیتابیس (همون پسورد بالا)
DB_PASSWORD=YOUR_STRONG_DB_PASSWORD

# OpenRouter API Key (از https://openrouter.ai/keys بگیر)
OPENROUTER_API_KEY=sk-or-v1-

# CORS Settings (دامنه فرانت‌اند خودت)
CORS_ALLOW_ALL_ORIGINS=False
CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com


# ===== ⚡ می‌تونی بعداً پر کنی =====

# SMS Provider Configuration
SMS_PROVIDER=faraz

# Faraz SMS (پنل پیامکی فراز) - https://farazsms.com
# برای دریافت این اطلاعات به پنل خودتون در farazsms.com مراجعه کنید
FARAZ_API_KEY=
FARAZ_ORIGINATOR=
FARAZ_PATTERN_CODE=

# Email Configuration (برای نوتیفیکیشن‌ها)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=


# ===== ✅ می‌تونن همینطوری بمونن =====

# Redis
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/1

# OTP Settings
OTP_EXPIRY_SECONDS=120
OTP_LENGTH=6
MAX_OTP_ATTEMPTS=5
OTP_BLOCK_DURATION=3600

# Security Settings (اتوماتیک فعال می‌شن با DEBUG=False)
SECURE_SSL_REDIRECT=True
SESSION_COOKIE_SECURE=True
CSRF_COOKIE_SECURE=True
SECURE_BROWSER_XSS_FILTER=True
SECURE_CONTENT_TYPE_NOSNIFF=True
SECURE_HSTS_SECONDS=31536000
SECURE_HSTS_INCLUDE_SUBDOMAINS=True
SECURE_HSTS_PRELOAD=True

# Database Connection Pooling
DB_CONN_MAX_AGE=600

# Logging
LOG_LEVEL=INFO
